claude-memory/
.claude/
.claude-start.sh
.DS_Store
._*

# Vendored binaries — large static builds (evermeet ffmpeg, go2rtc).
# Install fresh on a new machine via sidecar/macos/build_bundle.sh and
# the README, not via git.
bin/

# Swift Package Manager build artifacts (Path C avio-capture).
# Rebuild on a new machine via `cd sidecar/swift-capture && swift build -c release`.
sidecar/swift-capture/.build/
